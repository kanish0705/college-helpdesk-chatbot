# Requirements for College Helpdesk Chatbot
# Install these packages using: pip install -r requirements.txt

# Flask - Web framework for Python
# Used to create the web server and handle HTTP requests
flask==3.0.0

# OpenAI - For AI-powered responses (optional - for OpenAI provider)
# Only needed if LLM_PROVIDER = "openai" in config.py
openai==1.12.0

# Google Generative AI - For Gemini models (optional)
# Only needed if LLM_PROVIDER = "gemini" in config.py
google-generativeai==0.4.0

# Requests - For HTTP API calls (Groq and Gemini REST fallback)
requests==2.31.0

# Werkzeug - WSGI utilities (comes with Flask)
werkzeug==3.0.1

# python-dotenv - For loading environment variables (optional)
# Useful for keeping API keys secure
python-dotenv==1.0.0

# Gunicorn - Production WSGI server for deployment
# Required for hosting on Render, Railway, etc.
gunicorn==21.2.0
